import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentDeclaredAsIdError extends Error implements WithSource {
    type: string;
    name: string;
    id: string;
    sourceInfo: SourceInfo;
    fhirResourceUrl: string;
    constructor(type: string, name: string, id: string, sourceInfo: SourceInfo, fhirResourceUrl: string);
}
