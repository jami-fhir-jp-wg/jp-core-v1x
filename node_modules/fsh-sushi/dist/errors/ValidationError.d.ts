export declare class ValidationError extends Error {
    issue: string;
    fshPath: string;
    severity: ErrorSeverity;
    constructor(issue: string, fshPath: string, severity?: ErrorSeverity);
}
type ErrorSeverity = 'error' | 'warn' | 'info';
export {};
