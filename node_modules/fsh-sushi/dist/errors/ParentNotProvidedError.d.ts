import { Annotated } from './Annotated';
import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentNotProvidedError extends Error implements Annotated, WithSource {
    defnName: string;
    sourceInfo: SourceInfo;
    specReferences: string[];
    constructor(defnName: string, sourceInfo: SourceInfo);
}
