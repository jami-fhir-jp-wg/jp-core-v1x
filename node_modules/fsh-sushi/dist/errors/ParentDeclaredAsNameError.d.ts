import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentDeclaredAsNameError extends Error implements WithSource {
    type: string;
    name: string;
    sourceInfo: SourceInfo;
    fhirResourceUrl: string;
    constructor(type: string, name: string, sourceInfo: SourceInfo, fhirResourceUrl: string);
}
