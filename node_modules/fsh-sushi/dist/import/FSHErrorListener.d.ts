import { TextLocation } from '../fshtypes';
import { Recognizer, Token, ErrorListener } from 'antlr4';
export declare class FSHErrorListener extends ErrorListener<Token> {
    readonly file: string;
    constructor(file: string);
    syntaxError(recognizer: Recognizer<Token>, offendingSymbol: Token, line: number, column: number, msg: string): void;
    buildErrorMessage(recognizer: Recognizer<Token>, offendingSymbol: Token, line: number, column: number, msg: string): {
        message: string;
        location: TextLocation;
    };
}
